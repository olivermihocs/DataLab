# Local venv — for IDE type hints and autocompletion only.
# Actual execution happens inside Docker containers.
# Install with: pip install -r requirements.txt

pyspark==3.5.1
delta-spark==3.2.0
jupyterlab==4.2.*
boto3==1.34.*
pandas==2.2.*
pyarrow==15.*
matplotlib==3.9.*
seaborn==0.13.*
papermill==2.6.*
